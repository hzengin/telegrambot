telegram-bot {
  webhook {
    interface = 127.0.0.1
    port = 8080
    keystore = "/"
    keystorePassword = ""
  }
  polling {
    interval = 2
  }
  token = ""
}

spray.can.server {
  ssl-encryption = no
}
